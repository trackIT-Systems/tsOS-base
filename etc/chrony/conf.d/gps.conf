# Use GPS NMEA data via gpsd
refclock SHM 0:perm=0666 refid GPS precision 1e-1 offset 0.9999 delay 0.2

# Use PPS signal from gpsd for high accuracy (if available)
refclock SHM 1:perm=0666 refid PPS precision 1e-7
