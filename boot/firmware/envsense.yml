hardware:
  interval_s: 60
  sensors:
    # Raspberry Pi CPU temperature
    - id: soc
      type: rpi_cpu

daemon:
  csv:
    enabled: true
    path: /data
  mqtt:
    enabled: true
    host: localhost
    port: 1883
    qos: 0
